import { Component } from '@angular/core';
import { Filter } from '../filter/filter';

@Component({
  selector: 'filter-steps',
  imports: [Filter],
  templateUrl: './filter-steps.html',
  styleUrl: './filter-steps.css',
})
export class FilterSteps {}
